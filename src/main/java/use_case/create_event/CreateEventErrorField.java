package use_case.create_event;

/**
 * Identifies the create-event field associated with a validation error.
 */
public enum CreateEventErrorField {
    NONE, EVENT_NAME, START_DATE, START_TIME, END_DATE, END_TIME, END_SCHEDULE, EVENT_CURRENCY
}
